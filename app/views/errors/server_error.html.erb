<% body_id 'error' %>
<% section_image 'section-image-5' %>

<div class="row">
    <div class="wide">
        <h1 class="page-content-header">
            <span class="page-content-header-label"><%= t('errors.something_went_wrong') %></span>
        </h1>

        <p><%= t('errors.something_went_wrong_text') %></p>
        <p><%= t('pages.no_access.use_the_back_button') %> <%= link_to(t('global.listings'), listings_url) %>.</p>
    </div>
</div>
