<section id="surroundings-airport">
    <div class="mb10">
        <%= link_to(content_tag('i', '', class: 'icon-edit') + ' ' + t('global.edit'), edit_airport_listing_surrounding_path(listing),remote: true, class: 'btn btn-small', id: 'btn-edit-surrounding') %>
    </div>
    <article>
      <%= "#{listing.airport} - #{listing.distance}" unless listing.airport.nil? || listing.distance.nil? %> 
    </article>
</section>