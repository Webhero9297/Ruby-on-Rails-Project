<% body_id 'exchanges' %>
<div class="row-fluid">
  <div class="span12">
    <div class="plate">
      <div class="plate-content">
        <h1 class="plate-header">There are <%= @count %> with <%= @query %></h1>
        <%= render(partial: 'search_form', locals: {query: @query}) %>
        <%= render(partial: 'exchange_agreements_table', locals: {exchange_agreements: @exchange_agreements}) %>
        </table>
      </div>
    </div>
  </div>
</div>