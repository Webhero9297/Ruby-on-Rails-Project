$('[data-input="search-input"]').val('');
$('[data-list="saved-searches"]').replaceWith('<%= escape_javascript(render(partial: "searches/saved_searches_list", locals: {saved_searches: @saved_searches})) %>');