<tr id="<%= item.id %>">
  <td><%= item.destination %></td>
  <td><%= link_to(content_tag('i', '', class: 'icon-trash') + ' ' + t('button.delete'), account_wish_list_url(account.id, item.id), method: :delete, remote: true, confirm: "Are you sure you want to delete the destination") %></td>
</tr>